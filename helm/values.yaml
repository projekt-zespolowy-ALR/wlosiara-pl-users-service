port: 3000

postgresql:
  auth:
    database: "wlosiara-pl-users-service"
    username: "wlosiara-pl-users-service"
    password: null # required
  primary:
    initdb:
      scripts-configmap: "wlosiara-pl-users-service-initdb-scripts"
    resources:
      requests:
        cpu: "50m"
        memory: "128Mi"
    persistence:
      size: "1Gi"

image:
  dockerRegistry:
    host: null # required

resources:
  requests:
    cpu: "50m"
    memory: "128Mi"
  limits:
    cpu: "100m"
    memory: "256Mi"
